{
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.2.2",
    "fastify-socket.io": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "start": "npx tsc --build && node dist/index.js",
    "build": "npx tsc --build"
  }
}
